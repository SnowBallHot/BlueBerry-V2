[project]
name = "poker44-trace-detector"
version = "0.1.0"
description = "Poker44 SN126 detection miner (v2)"
requires-python = ">=3.10"
dependencies = ["bittensor", "numpy", "scikit-learn", "joblib", "torch"]

[tool.setuptools.packages.find]
include = ["poker44*", "neurons*"]
